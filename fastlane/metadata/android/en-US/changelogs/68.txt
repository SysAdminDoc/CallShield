A fix release after a review of 1.8.0. Category rules no longer act on a weak ML block. The Database and Blocked log filters keep their chips when Android closes the app, and a new filter never shows the last one's rows. A second outgoing call is no longer dropped when two overlap. Light theme contrast meets the AA minimum on the log cleanup chips, repeat counts and rule warnings, and telemarketing range names follow the app's language.
